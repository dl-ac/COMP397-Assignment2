module enums {
    export enum GameObjectType {
        ISLAND,
        CLOUD,
        BACKGROUND,
        BUTTON,
        IMAGE,
        PLAYER,
        PLAYER_BULLET,
        UNDEFINED,
        NUM_OF_TYPES
    }
}
