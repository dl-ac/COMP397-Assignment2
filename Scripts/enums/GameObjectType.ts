module enums {
    export enum GameObjectType {
        PLANE,
        ISLAND,
        CLOUD,
        BACKGROUND,
        BUTTON,
        PLAYER,
        BULLET,
        UNDEFINED,
        NUM_OF_TYPES
    }
}
