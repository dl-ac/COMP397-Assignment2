module enums {
    export enum GameObjectType {
        PLANE,
        ISLAND,
        CLOUD,
        BACKGROUND,
        BUTTON,
        PLAYER,
        PLAYER_BULLET,
        UNDEFINED,
        NUM_OF_TYPES
    }
}
